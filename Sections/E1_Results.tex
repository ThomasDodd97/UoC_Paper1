% ========================================================================================================================================================================================
\section{Results}
This work produced two sets of results: firstly, those derived from
 optimisation conducted via sampling directly from the latent objective
 function described by the physical system and secondly, those stemming
 \textcolor{RawSienna}{from the optimisation of the five surrogate functions}
 \sout{from the optimisation of a surrogate function}
 trained to emulate the latent objective function and the associated physical 
 system.
The latter set of results were generated to provide statistical support for
 the former.

% ========================================================================================================================================================================================
\subsection{Physical System Optimisation}
\textcolor{RawSienna}{A singular Bayesian sequential optimisation experiment
 was run, with a sample limit (\(p_{\text{tot}}=27\)), number of samples
 drawn per sequential iteration (\(p_{\text{iter}}=3\)), and a prior-forming
 dataset of 8 quasirandomly sampled targets 
 (Fig.~\ref{fig:BayesianPriorsAbbreviated}).
This Bayesian routine employed the probability of improvement acquisition
 function (Eq.~\ref{eq:ProbabilityOfImprovement}).
Figure \ref{fig:ResultOverview} illustrates the progression of this
 sequential routine when compared with the one-shot pseudorandom
 sampling technique, with the mass losses plotted on the y-axis being
 the best values discovered given the samples evaluated so far
 (\(\Delta m_{\%}^{\circ}\)).
A simple statistical review of the experimental datasets generated by 
 both the sequential Bayesian and one-shot pseudorandom techniques within
 the sample limit is provided in Table~\ref{tab:BayesianSequentialsBestValues}.
Given the sequential samples available (\(p_{\text{tot}}-p_{\text{init}}=19\))
 and the samples per iteration (\(p_{\text{iter}}=3\)), an extra iteration of 
 sequential sampling was run which consequently breached the sample limit-
 these extra samples are included in Figure \ref{fig:ResultOverview}, but
 are not considered in the preparation of Table 
 \ref{tab:BayesianSequentialsBestValues} (as it would be unfair to compare
 two different sampling techniques which were allowed an unequal number of
 samples over which to optimise).
}

\sout{
Figure \ref{fig:ResultOverview} shows that the sequential Bayesian technique 
 employing the `expected improvement' (EI - solid green line) acquisition 
 function discovered a point
 \textcolor{ForestGreen}{which makes a statistically significant improvement
 upon the strongest one-shot benchmark} exceeding the strongest one-shot 
 technique in almost half the allowable samples (63\% \(p_{\text{tot}}\)).
In contrast, the `probability of improvement' (PI - solid blue line) 
 acquisition function \textcolor{ForestGreen}{failed to make a statistically 
 significant improvement upon the grid sampled benchmark within the sample 
 limit, although the sample drawn on the sample limit 
 (100\% \(p_{\text{tot}}\)) did make significant improvements upon both the 
 pseudo/quasi-random benchmarks}; this was consistent with the 
 `probability of improvement' acquisition function exhibiting more 
 exploratory behaviour than the `expected improvement' acquisition function.
Further evidence of the `probability of improvement' acquisition function's
 exploratory bias is seen in Table \ref{tab:BayesianSequentialsBestValues},
 where the samples generated by this sequential routine obtain a markedly
 lower mean than that of those drawn by the `expected improvement'
 acquisition function; anecdotally, on reviewing the placement of samples 
 in 3D space it was noted that these were generally placed in exploratory
 locations within the parameter space.
Contrary to our initial expectations, the evidence presented above shows 
 that the `probability of improvement' acquisition function does not 
 exhibit overly exploitative behaviour due to the lack of a marginal 
 threshold parameter \(\varepsilon\) being manually tuned.
}

\begin{center}
    \captionsetup{format=custom,width=0.48\textwidth,skip=0pt}
    \includegraphics[width=0.48\textwidth]{PracticalResultOverview.png}
    \captionof{figure}{
        \footnotesize{
            Plot of optimisation progression for the experimentally
             deployed Bayesian optimisation routine utilising the
             probability of improvement (PI, solid blue line) 
             acquisition function, and compared alongside the 
             one-shot pseudorandom sampling technique 
             (PR, dashed line).
            Uncertainty in \(\Delta\text{m}_{\text{\%}}\) values
             is equal to 0.44\% points (\(1\sigma\)).
        }
    }
    \label{fig:ResultOverview}
\end{center}

\textcolor{RawSienna}{
Figure \ref{fig:ResultOverview} shows that although the sequential 
 Bayesian technique discovers a sample surpassing the pseudorandom
 benchmark by sample 21 (77\% of \(p_{\text{tot}}\)), its first 
 statistically significant improvement was recorded on sample 27
 (100\% of \(p_{\text{tot}}\))- this was consistent with 
 exploratory-biased behaviour.
Further evidence of the probability of improvement acquisition
 functions exploratory-biased behaviour is seen in 
 Table~\ref{tab:BayesianSequentialsBestValues}, where, despite an
 improvement upon the initial priors highest discovered target value
 (14.01\% points \(\rightarrow\) 16.67\% points), the running mean of
 samples within the growing dataset fell from its quasirandomly
 sampled prior's high (12.82\% points) to its final lowpoint (12.50\% 
 points) which reached an approximate parity with the pseudorandomly
 sampled benchmark dataset (12.54\% points).
Contrary to our initial expectations, the evidence presented above shows 
 that the probability of improvement acquisition function does not 
 exhibit overly exploitative behaviour due to the lack of a marginal 
 threshold parameter \(\varepsilon\) being manually tuned.
}

\sout{Two Bayesian sequential optimisation experiments were run, each with the same
 sample limit (\(p_{\text{tot}}=27\)), number of samples drawn per Bayesian 
 iteration (\(p_{\text{iter}}=3\)), and a prior-forming dataset of 8 quasirandomly
 sampled targets (See Figure \ref{fig:BayesianPriorsAbbreviated}).
The difference between these two methods was the acquisition function
 employed: the first using the `probability of improvement'
 (Equation~\ref{eq:ProbabilityOfImprovement}) whilst the second used the
 `expected improvement' function (Equation~\ref{eq:ExpectedImprovement}).
Figure \ref{fig:ResultOverview} shows the optimisation progression of these 
 two sequential techniques when compared with the one-shot deterministic 
 methods; mass losses plotted on the y-axis are the best values uncovered 
 given the samples evaluated so far, \(\Delta m_{\%}^{\circ}\).
A statistical overview is presented in Table 
 \ref{tab:BayesianSequentialsBestValues} which concerns the 8 quasirandom 
 and 16 sequential samples for each of the two Bayesian techniques.
}

\footnotesize
\begin{center}
    \begin{tblr}{colspec={Q[si={table-format=3.2},l]Q[si={table-format=3.2},l]Q[si={table-format=3.2},l]Q[si={table-format=3.2},l]Q[si={table-format=3.2},l]},row{1}={guard},column{1}={guard}}
        {Technique\\Function}&{No.}&{Max\\(\%)}&{Mean\\(\%)}&{Standard\\Dev. (\%)}\\
        \hline
        P  &   8     & 14.01 & 12.82 & 1.04 \\
        \hline[dashed]
        PI &   27     & 16.67 & 12.50 & 1.63 \\
        PR &   27     & 14.01 & 12.54 & 0.88 \\
    \end{tblr}
    \captionsetup{format=custom,width=0.48\textwidth,skip=0pt}
    \captionof{table}{
        \footnotesize{
            Statistical review of datasets generated experimentally
             within the sample limit.
            P: Prior dataset, quasirandomly sampled and 
             fed into Bayesian routine.
            PI: Final dataset from Bayesian routine
             using probability of improvement acquisition function.
            PR: Dataset from one-shot pseudorandom sampler.
            No.: Number of samples in dataset.
            Max: Highest discovered mass loss (\(\Delta m_{\%}^{\circ}\))
            Mean: Population mean.
            Standard Dev.: A single standard deviation.
        }
    }
    \label{tab:BayesianSequentialsBestValues}
\end{center}
\normalsize

% \begin{figure*}[htb]
%     \captionsetup{format=custom,width=0.8\textwidth,skip=0pt}
%     \includegraphics[width=\textwidth]{ResultOverview}
%     \captionof{figure}{
%         \footnotesize{
%             A plot comparing the optimisation progression for the two
%             Bayesian sequential techniques alongside the three one-shot
%             benchmark techniques.
%             Expected improvement (EI) - solid green line.
%             Probability of improvement (PI) - solid blue line.
%             One-shot techniques - dashed lines.
%             \textcolor{ForestGreen}{Uncertainty in 
%             \(\Delta\text{m}_{\text{\%}}\) values
%             is equal to 0.44\% points (\(1\sigma\)).}
%         }
%     }
%     \label{fig:ResultOverview}
% \end{figure*}

% By comparing Tables \ref{tab:BayesianSequentialsBestValues} and 
%  \ref{tab:BenchmarkBestValues}, it is apparent that both sequential routines
%  outperformed the one-shot techniques in terms of best sample discovered.

% \footnotesize
% \begin{center}
%     \begin{tblr}{colspec={Q[si={table-format=3.2},l]Q[si={table-format=3.2},l]Q[si={table-format=3.2},l]Q[si={table-format=3.2},l]},row{1}={guard},column{1}={guard}}
%         {Acquisition\\Function}&{Max\\(\%)}&{Mean\\(\%)}&{Standard\\Dev. (\%)}\\
%         \hline
%         PI & 16.67 & 12.50 & 1.63 \\
%         EI & 17.99 & 13.67 & 1.91 \\
%     \end{tblr}
%     \captionsetup{format=custom,width=0.48\textwidth,skip=0pt}
%     \captionof{table}{
%         \footnotesize{
%             Statistical overview of mass loss, \(\Delta m_{\%}\), target
%             variable evaluations obtained from the two 27-sample sequential
%             datasets: these either utilising the EI (`expected improvement’)
%             or PI (`probability of improvement’) acquisition functions
%             during Bayesian optimisation.
%             `Max' equates to the highest discovered mass loss,
%             (\(\Delta m_{\%}^{\circ}\)), `Mean' is the population mean, and
%             `Standard Dev.' relates to a single standard deviation.
%         }
%     }
%     \label{tab:BayesianSequentialsBestValues}
% \end{center}
% \normalsize

Notably, the Bayesian sequential technique employing the 'probability of
 improvement' acquisition function discovered samples attaining the highest
 mass losses of any sample characterised during this work
 (\(\Delta m_{\%}^{\circ}=20.77\%\)), albeit outside the sample limit
 (See Figure \ref{fig:ResultOverview}).

% ========================================================================================================================================================================================
\subsection{Emulated System Optimisation}

Since curing of a single prepolymer sample could take up to 
 \textcolor{RawSienna}{a week} \sout{2~weeks} to
 complete, the temporal expense of sampling the objective function was
 to high to allow for repeat experiments involving the sequential
 techniques deployed.
This precluded statistically proving the utility of Bayesian optimisation
 in this field \textcolor{RawSienna}{experimentally} within reasonable 
 timeframes.
However, by developing \sout{an} inexpensive surrogate functions capable 
 of emulating the physical system, a much larger number of function 
 evaluations could be carried out computationally, facilitating statistical 
 analysis of sampling methods' efficacy.
\textcolor{RawSienna}{
By despatching 1,000 simulations of any given sampling techique's ability
 to optimise any given emulator's surrogate function and returning an
 array of maximum target variable values \(\Delta m_{\%}^{\circ}\)
 discovered, basic statistical conclusions could be drawn.
All simulated sampling techniques observed a sample limit of \(p_{tot}=27\),
 whilst the sequential techniques were all initiated using \(p_{init}=8\)
 quasirandomly drawn samples.
Sampling techniques spanned one-shot and sequential forms, with the
 former group comprised of pseudorandom (PR) and quasirandom sampling (QR)
 whilst the latter was made up of both objective-naive and Bayesian methods.
The lone objective-naive space-filling techique simulated was the 
 mc-intersite-proj-th algorithm (MIPT), whilst the two Bayesian techiques 
 diverged in terms of the covariance function employed- either probability 
 of improvement (PI) or expected improvement (EI).
}
\sout{
By despatching 1,000 simulations of each of the two random benchmark one-shot
 techniques and the two Bayesian sequential techniques, and returning the
 best target variable values \(\Delta m_{\%}^{\circ}\) discovered by each
 of these, basic statistical conclusions could be drawn.
Note that each simulation of the sequential techniques was initiated 
 with a separate quasirandomly drawn prior of 8 samples and that the grid
 sampler was deprecated since its outcomes were not affected by chance.
}

\begin{figure*}[htb]
    \captionsetup{format=custom,width=0.8\textwidth,skip=0pt}
    \includegraphics[width=\textwidth]{ComputationalResultOverview.png}
    \captionof{figure}{
        \footnotesize{
            Strip plots of maximum function evaluations obtained
             \(\Delta m_{\%}^{\circ}\), for each of the 25,000
             simulated sampling exercises undertaken.
            Every simulated sampling exercise observed a constant
             sample limit \(p_{tot}=27\), with each sampling
             technique being simulated 1,000 times upon each of
             the emulators, and the Bayesian routines were initiated
             with 8 quasirandomly drawn samples.
        }
    }
    \label{fig:CompResOverv}
\end{figure*}

% \begin{figure*}[htb]
%     \captionsetup{format=custom,width=0.8\textwidth,skip=0pt}
%     \includegraphics[width=\textwidth]{SimulationResultOverview}
%     \captionof{figure}{
%         \footnotesize{
%             Swarm plot of maximum function evaluations obtained,
%             \(\Delta m_{\%}^{\circ}\), for each of the 4,000 simulated 
%             optimisations carried out.
%             All optimisations used 27 samples, each technique was 
%             simulated 1,000 times, and the Bayesian routines were
%             initiated with 8 quasirandomly sampled points.
%         }
%     }
%     \label{fig:SimulationResultOverview}
% \end{figure*}

\textcolor{RawSienna}{
Figure~\ref{fig:CompResOverv} presents a series of strip plots showing
 distribution of outcomes for the 25,000 simulations undertaken across
 the five sampling techniques and five emulators studied.}
On comparison of the mean \(\Delta m_{\%}^{\circ}\) values discovered by the
 various sampling techniques, sequential Bayesian techniques were shown to
 generally perform better
\textcolor{RawSienna}{ (as observed in 4/5 emulators trialled) than either
 the one-shot or objective naive sequential techniques.}
Although standard deviation values associated with these mean values would
 preclude a straightforward conclusion that the difference between values
 are statistically significant, it can be seen that the simulations are 
 distributed in a somewhat lognormal fashion which shows that only the 
 Bayesian techniques are capable of discovering well-above-average-performing 
 samples (causing the majority of skew seen in the means).

To investigate the degree to which exceptionally high value formulations
 were uncovered by the various sampling techniques, a `region of global 
 optimality' was devised for each of the emulators, with an upper bound 
 defined by the computational global maximum and a lower bound defined as 
 being 10\% lower than this (as relativised by the computational global 
 minimum).
The Bayesian sequential technique adopting the expected improvement
 acquisition function in its sampling policy not only outperformed the
 probability of improvement technique in terms of mean values attained,
 but also in number of simulations obtaining \(\Delta m_{\%}^{\circ}\)
 values which resided within the small region of global optimality,
\textcolor{RawSienna}{as evidenced in Table~\ref{tab:CompResOvervTab}.
Notably, neither of the one-shot techniques nor the objective-naive
 sequential technique were able to produce as high a proportion of 
 samples within the `region of global optimality', with all three
 consistently producing either one or two orders of magnitude less
 samples than that generated by the Bayesian techniques 
 (Tab.~\ref{tab:CompResOvervTab}).}

\textcolor{RawSienna}{Taking the GP-Mat-05 as the most accurate of the 
 emulators}, a (particularly) tentative assumption that the final 
 emulation was an accurate description of the underlying objective function,
\textcolor{RawSienna}{and a further assumption that the aforementioned
 `region of global optimality' is a reasonable proxy for the experimental
 global optimum discovered during the practical work},
 we could venture that the probability of having discovered 
\textcolor{RawSienna}{this formulation} using the expected improvement 
 and probability of improvement techniques was 
\textcolor{RawSienna}{65.0\% and 23.8\% respectively.}
This would make the practical reality of having discovered these
\textcolor{RawSienna}{exceptionally well-performing formulations during
 the course of our one-off implementation of the Bayesian sequential
 routine improbable but certainly not impossible; and, importantly the
 probabilities are a marked improvement upon the 0.6\%, 1.3\%, and 0.5\%
 values exhibited by the two one-shot techniques (PR and QR) or the
 objective-naive MIPT technique respectively.
}

\sout{
Figure --- presents a swarmplot showing
 distribution of outcomes for the 4,000 simulations across the four sampling
 techniques studied.
(PI=14.6\%, EI=15.4\% for
 \(\Delta m_{\%}^{\circ}\)) than one-shot techniques (PR=14.0\%, 
 QR=14.1\% for \(\Delta m_{\%}^{\circ}\)).
This region was characterised as containing function evaluations exceeding
 18.5\%, and this simple threshold was therewith used to calculate the 
 number of simulations attaining such globally optimum values
 (\(\Delta m_{\%}^{\circ}>18.5\)) for each of the sampling techniques
 (PI=11, EI=53 simulations where \(\Delta m_{\%}^{\circ}>18.5\)).
Notably, neither of the one-shot techniques was able to uncover the
 globally optimum region (PR=0, QR=0 simulations where 
 \(\Delta m_{\%}^{\circ}>18.5\)).
Given the above, and \dots we could venture that the probability of having
 discovered the experimental global optimum \dots 5.3\% and 1.1\% respectively.
}

\footnotesize
\begin{center}
    \begin{tblr}{colspec={Q[si={table-format=3.2},l]Q[si={table-format=3.2},c]Q[si={table-format=3.2},c]Q[si={table-format=3.2},c]Q[si={table-format=3.2},c]Q[si={table-format=3.2},c]},row{1}={guard},column{1}={guard}}
        {Emu.}&{PR \\ (\%)}&{QR \\ (\%)}&{MIPT \\ (\%)}&{PI \\ (\%)}&{EI \\ (\%)}\\
        \hline
        M05    & 0.6    & 1.3       & 0.5   & 23.8  & 65.0 \\
        M15    & 6.9    & 5.9       & 3.4   & 26.3  & 69.9 \\
        M25    & 9.1    & 6.9       & 5.3   & 26.6  & 59.3 \\
        Rbf    & 65.2   & 71.1      & 53.0  & 96.8  & 100.0 \\
        Xgb    & 0.8    & 0.4       & 0.4   & 1.3   & 5.1 \\
    \end{tblr}
    \captionsetup{format=custom,width=0.48\textwidth,skip=0pt}
    \captionof{table}{
        \footnotesize{
            Summary of percent of simulations surpassing the
             lower region of optimality bound.
            Emu.: Emulators simulated upon.
            MO5/M15/M25: Gaussian process emulators
             where \(\nu=0.5/1.5/2.5\) respectively.
            Rbf: Gaussian process emulator with RBF kernel.
            Xgb: XGBoost gradient boosted decision tree emulator.
            Avg.: Average values across the various emulators.
        }
    }
    \label{tab:CompResOvervTab}
\end{center}
\normalsize
